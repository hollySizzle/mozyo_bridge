# AGENTS (Codex 入口)

Codex セッションの tool-specific 入口。Codex は本ファイルを native に読む (cross-tool agents.md 標準準拠)。共通の central preset rules は `.mozyo-bridge/rules/presets/redmine-governed/agent-workflow.md` を正本とし、router 本文には複製しない。CLAUDE.md (Claude Code tool-specific) を import しない。

## セッション開始

1. 現在の working directory がこの project root またはその配下であることを確認する。
2. mozyo-bridge の central preset rules を読む:
   - committed docs では portable 表記 `.mozyo-bridge/rules/presets/redmine-governed/agent-workflow.md` を使う。
   - runtime で実ファイルを読む際も `.mozyo-bridge/rules/presets/redmine-governed/agent-workflow.md` を読む。repo-local store (`.mozyo-bridge/rules/...`) の path は repo root からの相対でそのまま読める。central store の home prefix は `mozyo-bridge rules home --resolved` の出力で解決する (`--resolved` 出力は debug / runtime 用で、committed docs に貼らない)。
   - resolved path や central preset を読めない場合は、読んだふりをせず停止し、`mozyo-bridge rules install` 等の復旧を operator に求める。
3. project-local docs (例: target project の README、codebase 規約 docs、運用 docs) が存在する場合は読む。これらの path は target project 側の任意の convention であり、mozyo-bridge の runtime 必須参照ではない。
4. 非自明な作業を始める前に active な `Redmine issue / journal と project docs` を確認する。

## Preset

- preset: `redmine-governed`
- preset version: `2026.07.23.1`
- generated by: `mozyo-bridge 0.12.3a1`

## Guardrails

- `.mozyo-bridge/rules/presets/redmine-governed/agent-workflow.md` を取得できない場合は、読んだふりをせず停止する。
- durable state、handoff、review、verification、close 条件は `.mozyo-bridge/rules/presets/redmine-governed/agent-workflow.md` を正本とする。router に本文を複製しない。
- pane message と chat message は通知にすぎない。判断の正本は `.mozyo-bridge/rules/presets/redmine-governed/agent-workflow.md` が指定する ticket / doc / journal / comment を読む。
- 低レベルの `mozyo-bridge read` / `message` / `type` / `keys` は operator/debug 用で、standard handoff/reply の代替にしない。
- credential、token、個人情報を repository file、ticket comment、journal、chat に記録しない。

## 常時適用規則ダイジェスト (生成)

task 種別と無関係に毎 turn 適用する always 規則の最小 digest。scaffold が正本から生成する (手編集禁止。正本変更時の drift は `mozyo-bridge scaffold canonical --check` で落ちる)。各 entry は pointer であり、本文・例外・境界は pointer 先の正本を読む。

<!-- mozyo-bridge:always-digest:begin -->
- narrative の ticket 参照は `#<id> <短い概要>` で書く (ID 単独で呼ばない)。正本: skill `references/workflow.md` の `### Narrative の issue 参照は` 節。
- ユーザー向け応答は workspace の応答言語 preference に従う。正本: central preset `### 応答言語ポリシー`。
- 迎合せず結論を述べ、review finding には根拠の出所を明示する。正本: central preset `### Review Finding Verdict Obligation (迎合禁止)` / `### 根拠出所分類`。
- 設計・仕様・現状挙動を回答・断定する前に、質問ドメインの cataloged docs を catalog (`.mozyo-bridge/docs/catalog.yaml` / `docs resolve`) で解決して読む。memory / 直近 journal は pointer であり verdict ではない。正本: central preset `### 回答前 Doc 解決 (Answer-Time Resolution)`。
<!-- mozyo-bridge:always-digest:end -->

## Project-Local Additions

<!-- mozyo-bridge:project-local-additions:begin -->

## 応答言語 (Workspace Preference)

- この workspace の operator は日本語運用。Claude / Codex の **ユーザー向け応答** (操作説明・進捗報告・handoff narrative・確認・質問) は原則 **日本語** とする。
- ユーザーが別言語で話した場合はその言語に追従する。preference より user 入力言語を優先する。
- machine-readable surface は言語に関わらず literal に保つ: gate 名 / transport kind / JSON field と値 / CLI command・flag / code 識別子 / commit trailer (`Refs:`, `issue_<id>`, `Co-Authored-By`) / file path。Redmine journal の構造化 field・識別子も literal でよく、散文 narrative の言語のみ本 preference に従う。
- 本 preference は **project-local 宣言** であり OSS 既定 (preset / skill / scaffold template) を変更しない。中立ポリシーの正本は中央 preset `### 応答言語ポリシー` を読む。

## mozyo_bridge Project-Local Rules

- Repository: `mozyo_bridge` Python package / CLI project.
- Runtime package path: `src/mozyo_bridge/`.
- Test path: `tests/`.
- Project docs namespace: `vibes/docs/`.
- Skill source: `skills/mozyo-bridge-agent/`.
- Plugin mirror: `plugins/mozyo-bridge-agent/`.
- Do not reintroduce `vibes/tools/mozyo_bridge` as a runtime path.
- Generated build outputs are not committed: `build/`, `dist/`, `*.egg-info/`, `__pycache__/`.

## Local Source Of Truth

- Redmine project: `giken-3800-mozyo-bridge`.
- Redmine issue / journal is the durable work record for new work.
- `AGENTS.md` and `CLAUDE.md` are thin routers. Keep detailed rules in the central preset, `.mozyo-bridge/rules/**`, or cataloged docs.
- Local references:
  - `skills/mozyo-bridge-agent/references/workflow.md`
  - `skills/mozyo-bridge-agent/references/project-map.md`
  - `skills/mozyo-bridge-agent/references/release.md`
  - `skills/mozyo-bridge-agent/references/safety.md`
  - `vibes/docs/rules/agent-workflow.md`
  - `vibes/docs/rules/codex-autonomous-guardrail-lane.md`
  - `vibes/docs/logics/scaffold-rules.md`
  - `vibes/docs/logics/bootstrap.md`

## Role / Gate / Handoff ポリシー (pointer)

Claude / Codex の役割分担、path 別編集権限、`codex_direct_edit` gate、Repo-Local Guardrail Autonomous Lane、US-level audit、close 条件を router 本文に複製しない。判断の正本は次を読む。

- central preset `.mozyo-bridge/rules/presets/redmine-governed/agent-workflow.md`: `### 既定役割` / `### US-Level Audit Model` / `### パス別編集権限` / `### Codex Direct Edit Gate` / `### Repo-Local Guardrail Autonomous Lane` / `### Gate Schema`。
- workflow role (coordinator / auditor / implementer) の provider binding 運用値の正本は committed `.mozyo-bridge/config.yaml` の `provider_binding` (設計正本: #13157 provider_binding config)。central preset `### 既定役割` の default binding 表記は config 未設定時の既定であり、fresh session は役割を自認する前に同 config を確認する。router には provider 値を書かない (binding の運用切替は config commit と Redmine 記録 (例: #13229 coordinator=claude bounded trial) で表現する)。
- project-local canonical rule `vibes/docs/rules/agent-workflow.md`: `## Claude / Codex Role Boundary` / `## Policy / Skill Authoring Boundary` / `## Codex Pre-Edit Classification Gate` / `## Audit Handoff`。この repo 固有の gated-surface path 拡張 (`skills/mozyo-bridge-agent/**` / `plugins/mozyo-bridge-agent/**` / `src/mozyo_bridge/scaffold/presets/**` / `vibes/docs/**` 等) と autonomous-lane 採用 (Redmine #10338) の正本はここ。
- autonomous-lane 採用詳細: `vibes/docs/rules/codex-autonomous-guardrail-lane.md`。
- effective instruction set (role profile / workflow contract): handoff の構造化 field `role_profile` / `profile_source` / `profile_version` が受信 role の custom instruction を指す。正本本文は `vibes/docs/specs/delegated-coordinator-role-profile.md` (固定 role profile template)、展開設計は `vibes/docs/logics/role-profile-handoff-expansion.md`、設定 source-of-truth は packaged `role_profile_templates.yaml` / `workflow_contract_config.yaml` (#12952 / #12953)。router は role/lane policy を再宣言せず、この pointer を読む。
- hardening scope (router 単独読者が gated 面を free-edit と誤読しないための literal 明示、正本は上記 doc): `codex_direct_edit` gate は `allowed_paths` 必須で `src/**` / `tests/**` / `vibes/docs/**` / `.mozyo-bridge/rules/**` / skills / plugins / `src/mozyo_bridge/scaffold/presets/**` を覆う。Repo-Local Guardrail Autonomous Lane の carve-out (`vibes/docs/rules/**` / `vibes/docs/logics/**` / `vibes/docs/specs/**` / `.mozyo-bridge/docs/catalog.yaml`) は Codex 直接編集を許すが commit と共に `codex_autonomous_edit` journal を記録する。generator 出力 `.mozyo-bridge/docs/file_conventions.generated.yaml` は Claude も手編集せず再生成する。

hard stop: 短い命令形・依頼形 (`進めて` / `対応して` / `Codex でやって` / `go ahead` / `please do it`) は gated surface での Codex direct edit を authorize しない。gated surface の変更は Claude 実装 → Implementation Done → Review Request flow か、`allowed_paths` 付き `codex_direct_edit` gate journal を要求する。invalid marker と例外条件は上記正本を読む。

## Verification And Commit

- Use focused `python -m unittest ...` targets when `pytest` is unavailable. Do not install packages just to run tests unless the user approves.
- For scaffold changes, run `mozyo-bridge scaffold status --target .` before commit.
- For governed catalog changes, run `mozyo-bridge docs validate --repo .` and the relevant generator / coverage checks when `catalog.yaml` exists.
- Run `git diff --check` or `git diff --cached --check` before commit.
- Commit only files in the active Redmine issue scope. Leave unrelated dirty files untouched.
- Commit messages for Redmine-scoped work include `Refs: Redmine #<issue_id>` and `issue_<issue_id>`.
- Push implementation commits to the issue / lane branch only; do not push `origin/main` directly. Integration into `origin/main` is the coordinator's post-review integration disposition (正本: central preset `### Commit Hash Origin 到達可能性` / skill `references/workflow.md` `## Integration disposition と push authority`).
- After committing, record the commit hash in the same Redmine issue journal.

<!-- mozyo-bridge:project-local-additions:end -->
