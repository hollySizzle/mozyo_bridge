# Module-health gate config (Redmine #12321; ratchet US #12324).
#
# Read by `mozyo-bridge health check` / `mozyo-bridge health report`. The gate
# enforces a PyLint-`too-many-lines`-equivalent threshold over the runtime
# package and records the existing oversized modules here instead of splitting
# them (full legacy teardown is an explicit non-goal). See
# vibes/docs/logics/module-health-gate.md for the full contract.
#
# Gate behavior:
#   - a module over `max_module_lines` that is NOT listed below fails the gate
#     (new oversized file);
#   - a listed module whose current line count exceeds its `lines` baseline
#     fails the gate (existing oversized file grew);
#   - a listed, still-oversized module whose `expires` deadline has passed fails
#     the gate (#12324 expiry ratchet) — the exception cannot live forever;
#   - `lines` is the recorded baseline; lower it (with justification) only when
#     the module actually shrinks.
#
# Every entry MUST carry a Redmine `owner_issue` AND an `expires` deadline
# (#12324 acceptance). `expires` is the date the exception lapses; it is set to
# the planned resolution Version's due date. Within `expiry_warning_days` of the
# deadline the gate emits a non-fatal "expiring soon" warning so Review Request /
# Version close readiness sees the impact before the deadline, not only after.
#
# `resolution_version` records the planned release each oversized module is
# scheduled to be reduced in (coordinator decision, #12321 j#62668):
#   - presentation_grouping.py -> v0.10.8  (Version #239, US #12322) — DONE: split
#     into the `domain/presentation_grouping/` subpackage (config / validation /
#     authority guard / placement / degraded submodules), so it is no longer
#     oversized and its allowlist entry has been removed.
#   - cockpit_ui.py            -> v0.10.9  (Version #240, US #12323) — DONE: split
#       into cockpit_page.py / cockpit_payload.py / cockpit_actions.py and removed
#       from this allowlist; cockpit_ui.py is now a thin facade.
#   - the remaining six        -> v0.10.10 (Version #241 ratchet, US #12324),
#       due 2027-11-30 — each `expires` below tracks that Version due date. The
#       splits themselves are deferred to per-module follow-up issues under that
#       Version; this gate stops *new* growth and forces resolution by the
#       deadline in the meantime.

max_module_lines: 1000

# Days before an `expires` deadline at which the gate starts warning (non-fatal).
expiry_warning_days: 30

include:
  - src/mozyo_bridge

allowlist:
  - path: src/mozyo_bridge/application/commands.py
    lines: 5931
    reason: >-
      Monolithic command-handler module; the CLI parser split (#12138/#12141/
      #12153/#12155) moved registration out but the handler bodies still live
      here. Largest decomposition target. Baseline raised 5720 -> 6072 on
      integrating already-reviewed #12341 cockpit membership list/status CLI
      handlers into this module (#12321 j#62683). Raised 6072 -> 6136 for the
      #12388 role-profile resolution wiring in `orchestrate_handoff` (fail-closed
      resolve + threading the resolution through the typed terminal paths).
      Raised 6136 -> 6157 for the #12466 delegated-coordinator-tree display
      columns in `cmd_agents_targets` (derive once + render KIND/DEPTH/PARENT in
      text and the additive JSON `delegation` record; the derivation itself lives
      in the new `domain/delegation_display.py`, not here).
      Raised 6157 -> 6205 for the #12467 delegated-coordinator window-policy
      display wiring in `cmd_agents_targets` (per-repo fail-soft
      `delegation_window_policy` resolution + the additive JSON
      `delegation_window` sibling; the policy schema/resolver live in
      `domain/presentation_grouping/`, not here).
      Raised 6205 -> 6270 for the #12580/#12581 queue-enter Enter-only retry in
      `orchestrate_handoff` (re-issue Enter on the policy interval until the
      marker is observed or the window elapses, marker+body typed once; the
      retry probe shares the new `_marker_visible_in` one-shot matcher and the
      policy/telemetry types live in `domain/handoff.py`, not here).
      Raised 6270 -> 6389 for the #12597 standard_target_admission rail in
      `orchestrate_handoff` (replace the Step 11 active-split fail-closed with
      the admission gate + `select-pane` activation/restore via the new
      `_activate_target_pane` / `_window_active_pane_id` helpers, threading the
      `TargetActivationOutcome` through the emit/persist paths; the admission /
      activation / policy types live in `domain/handoff.py`, not here).
      Raised 6389 -> 6552 for the #12658 project-scoped cockpit identity wiring
      (the `_resolve_project_scope_fields` helper + project-scope resolution
      threaded through the three cockpit-workspace construction sites and
      `_agents_target_candidates`, the appended PROJECT/PROJECT_PATH columns in
      `cmd_agents_targets`, and the additive `target_project` handoff gate; the
      discovery / cache / drift policy itself lives in the new
      `f_110_workspace_session_identity/{domain,application}/project_*` modules,
      not here).
      Raised 6552 -> 6592 for the #12658 review j#66481 fixes in the
      `target_project` handoff gate (require an explicit `--target-repo` gate
      under `--target-project`, and verify a stamped pane-option scope's cwd is
      under the stamped project path before trusting it).
      Raised 6592 -> 6607 for the #12658 j#66499 scenario fix: project-scope
      resolution prefers the real Git worktree root over a nested project-local
      `.mozyo-bridge/scaffold.json` (via the new `resolve_workspace_root`) in
      `_resolve_project_scope_fields` and the `target_project` gate, so cockpit
      dry-run / handoff from a project subdir emit the Git root + a repo-relative
      project path while non-git scaffold workspaces are preserved.
      Raised 6607 -> 6636 for the #12658 j#66504/j#66505 fixes: the handoff
      `--target-repo` / `--target-repo auto` repo gates also use the
      Git-root-preferring resolver, and project-scoped cockpit columns thread a
      `launch_cwd` (project workdir) through the 3 workspace-construction sites so
      a launched pane's cwd is under the project path.
      Raised 6636 -> 6656 for the #12708 live project-gateway lane identity
      projection in `cmd_agents_targets` (the additive JSON `gateway` record + the
      appended TARGET_KIND text column distinguishing project_gateway /
      workspace_root / worker; the classifier, identity, and launch-or-adopt
      policy live in the new
      `f_120_agent_discovery_pane_resolution/domain/project_gateway_identity.py`,
      not here).
      Raised 6656 -> 6697 for the #12706 ticketless transition role/action
      boundary wiring in `orchestrate_handoff` (fail-closed `resolve_transition_role`
      of the `args.transition_role` token + threading the boundary through
      `build_notification_body` and the typed terminal `make_outcome` paths; the
      boundary values + resolver live in the new
      `f_130_handoff_routing/domain/transition_role.py`, not here).
      Raised 6697 -> 6747 for the #12700 workflow-contract reference wiring in
      `orchestrate_handoff` (fail-closed `resolve_workflow_contract` of the
      `args.workflow_contract` token + threading the bundle through
      `build_notification_body` and the typed terminal `make_outcome` paths, plus
      restoring the #12706 `transition_role` on the successful-delivery
      `make_outcome` it had been dropped from; the bundle values + resolver live in
      the new `f_130_handoff_routing/domain/workflow_contract.py`, not here).
      Raised 6747 -> 6827 for the #12703 ticketless no-anchor callback rail in
      `orchestrate_handoff` (the `ticketless=True` path builds a fail-closed
      `TicketlessCallback` + `TicketlessAnchor` instead of `normalize_anchor`, and
      threads the callback through `build_notification_body` and the typed terminal
      `make_outcome` paths; the callback values + worker-dispatch refusal live in
      the new `f_130_handoff_routing/domain/ticketless_callback.py`, not here).
      Raised 6827 -> 6857 for the #12705 LLM-facing q-enter / submit-complete
      front-door telemetry wiring in `orchestrate_handoff` (the `_submit_lines_for`
      helper + the `submit_lines` kwarg threaded into `_emit_outcome` at the three
      post-typing emit sites). The front-door brain (intent -> rail / anchor
      resolution, composer-residue classification, delivery-id) lives in the new
      `f_130_handoff_routing/domain/q_enter.py` and the handler in
      `f_130_handoff_routing/application/cli_handoff_q_enter.py`, not here.
      Raised 6857 -> 6879 for the #12739 cockpit project-scope coexistence fix:
      `_read_cockpit_columns` now reads the #12658 `@mozyo_project_scope` /
      `_path` / `_label` pane triple, `cmd_cockpit`'s duplicate/focus `same`
      lookup adds the normalized `project_scope` to the unit key (so a
      department-root pane and a project-gateway pane sharing the same Git root +
      lane coexist instead of collapsing into a focus), and the `cockpit --json`
      payload projects `project_scope`. Pure read-model + matching-key change; no
      new domain module.
      Raised 6879 -> 6930 for the #12740 forward ticketless no-anchor consultation
      branch in `orchestrate_handoff`: a `ticketless_consultation` mode that builds
      the `TicketlessConsultation` payload + `TicketlessConsultationAnchor` (fail-
      closed on an unknown token, so a worker dispatch cannot ride this rail) and
      threads the forward payload through `build_notification_body` and the typed
      terminal `make_outcome` paths alongside the #12703 callback. The forward
      consultation values + worker-dispatch anchor invariant live in the new
      `f_130_handoff_routing/domain/ticketless_consultation.py`, and the
      `project-gateway consult` handler in
      `f_120_agent_discovery_pane_resolution/application/cli_project_gateway.py`,
      not here.
      Raised 6930 -> 6983 for the #12748 forward ticketless parent -> child
      work-intake branch in `orchestrate_handoff`: a `ticketless_work_intake` mode
      that builds the `TicketlessWorkIntake` payload + `TicketlessWorkIntakeAnchor`
      (fail-closed on an unknown token; a worker dispatch cannot ride this rail) and
      threads the forward payload through `build_notification_body` and the typed
      terminal `make_outcome` paths alongside the #12740 consultation / #12703
      callback. The work-intake values + invariants live in the new
      `f_130_handoff_routing/domain/ticketless_work_intake.py`, the same-lane child
      resolver in `f_120_.../domain/child_intake_route.py`, and the
      `project-gateway child-intake` handler in
      `f_120_.../application/cli_project_gateway_child_intake.py`, not here.
      Lowered 6983 -> 6902 for the #12749 OOP-first first-conversion tranche: the
      `tmux-ui-config` / `tmux-ui install|uninstall|status` command family moved
      to the new `application/commands_tmux_ui.py` (typed request/result value
      objects + an `ApplyTmuxConfigUseCase` taking the new
      `application/tmux_control_port.py` `TmuxControlPort` by injection, with a
      fake-port test) and `config_path_from_args` moved to `commands_common.py`.
      `commands.py` re-exports the `cmd_*` entry points so the public import /
      monkeypatch / parser-binding surface is unchanged. Baseline ratcheted to the
      post-extraction size; residual procedural orchestration (cockpit / handoff /
      agents / status / notify / init) is carried to #12638 / #12785.
      Lowered 6902 -> 6782 for the #12749 second OOP-first tranche: the `agents
      attention-project` handler + the shared `_attention_for_candidate` helper
      moved to the new `application/commands_agents.py`, with the tmux pane-option
      *write* behind the new `application/tmux_option_port.py` `TmuxOptionWriterPort`
      driven by a `ProjectAttentionUseCase` returning typed `AttentionProjectionEntry`
      value objects (fake-port test; the old `commands.run_tmux` monkeypatch seam for
      this write is retired). `commands.py` re-exports both symbols (identity / parser
      binding / `_attention_for_candidate` import preserved). The shared
      `_agents_target_candidates` discovery pipeline and the `agents list` / `agents
      targets` read handlers remain here as residual to #12638 / #12785.
      Lowered 6782 -> 6722 for the #12749 third OOP-first tranche (read-discovery):
      the `_agents_target_candidates` candidate pipeline orchestration moved to the
      new `ResolveAgentTargetsUseCase` (in `commands_agents.py`) depending on the new
      `application/agent_discovery_port.py` `AgentDiscoveryPort` (discover / canonical
      session / checkout facts / project scope) with a `LiveAgentDiscovery` adapter
      and a fake-port use-case test. `commands._agents_target_candidates` is now a thin
      wrapper (public name kept for the 6 delegated-coordinator / project-gateway
      importers and the tests that patch it); behavior byte-identical. The live adapter
      still routes the `commands`-owned leaf reads (`resolve_canonical_session` /
      `_probe_checkout_facts`) at call time as a documented compatibility bridge —
      relocating those leaf reads and the `agents list/targets` render handlers stays
      residual to #12638 / #12785.
      Lowered 6722 -> 6472 for the #12749 fourth OOP-first tranche: the `agents
      list` / `agents targets` render handlers (`cmd_agents_list` /
      `cmd_agents_targets`) moved to `commands_agents.py` (behavior-preserving;
      re-exported so the cli / cli_agents parser bindings and import identities are
      unchanged). `cmd_agents_targets` reaches the residual discovery seam through
      the call-time `_discover_candidates` helper (so the `commands.*` discovery
      patches still apply) and its availability guard is now `commands_agents.require_tmux`
      (4 test patch sites migrated).
      Lowered 6472 -> 6455 for the #12749 fifth tranche: the legacy read-only `list`
      handler (`cmd_list`) moved to `commands_agents.py` (re-exported as
      `commands.cmd_list`; cli_core / cli parser binding unchanged). Its
      `require_tmux` / `pane_lines` guard is now bound in `commands_agents`, so its one
      test (`test_cmd_list_emits_window_column_in_place_of_label`) patches
      `commands_agents.require_tmux` / `commands_agents.pane_lines`. The
      `commands`-owned leaf reads (`resolve_canonical_session` / `_probe_checkout_facts`)
      and the remaining orchestration (cockpit / handoff / status / notify / init)
      stay residual to #12638 / #12785.
      Lowered 6455 -> 6369 for the #12749 sixth tranche: the read-only inventory
      list handlers `cmd_workspace_list` and `cmd_session_list` moved into the
      `f_110_workspace_session_identity` numbered package (the documented "later
      wave" — `cmd_workspace_list` joined `commands_workspace.py` next to
      `cmd_workspace_defaults`; `cmd_session_list` opened a new `commands_session.py`).
      Both re-exported via `commands.py` so the cli / cli_workspace / cli_session
      parser bindings and the workspace/session test imports are unchanged; neither
      handler touches a `commands.*`-patched primitive, so no test patch migration was
      needed.
      Lowered 6369 -> 6125 for the #12749 seventh tranche: the remaining read-only
      session / workspace identity handlers moved into the `f_110` numbered package
      — `cmd_session_name` / `cmd_session_boundary_prompt` / `cmd_session_pane_decision`
      into `commands_session.py`, and `cmd_workspace_inspect` into `commands_workspace.py`.
      All re-exported via `commands.py` (cli / cli_session / cli_workspace bindings and
      the test_workspace_registry / test_session_boundary imports unchanged). None
      relies on a `commands.*`-patched primitive (resolve_canonical_session etc. are
      real or patched at the `workspace_registry` source), so no test patch migration
      was needed.
      Lowered 6125 -> 6026 for the #12749 eighth tranche: the low-risk write-surface
      handlers moved into the `f_110` numbered package — `cmd_workspace_register` into
      `commands_workspace.py` (completing the workspace family) and
      `cmd_session_vscode_settings` into `commands_session.py` (completing the session
      family). Both re-exported via `commands.py` (cli / cli_session / cli_workspace
      bindings + test_workspace_registry import unchanged); neither touches a
      `commands.*`-patched primitive (file / registry writes are real or through
      lazy domain imports), so no test patch migration was needed. The full workspace
      and session command families now live in the `f_110` package; the remaining
      `commands.py` orchestration (cockpit / handoff / status / notify / init) stays
      residual to #12638 / #12785.
      Lowered 6026 -> 6025 for the #12785 first OOP-first command-boundary tranche:
      the `status` session-read boundary (existence / window enumeration / pane
      capture + present-missing agent-window classification) moved behind
      `StatusSessionPort` + `ResolveSessionStatusUseCase` (`status_session_port.py` /
      `commands_status.py`); `cmd_status` stays here as a thin handler driving the
      use case via `LiveStatusSession` (the live adapter routes the three reads
      through `commands.*` at call time, so the existing status monkeypatch
      integration tests are unchanged). The new fake-port unit test
      (`test_status_session_use_case`) is the monkeypatch -> fake-port migration for
      that boundary. The `cmd_doctor` tail and `_status_repo_cockpit_membership`
      cockpit projection stay residual to #12638.
      Lowered 6025 -> 5984 for the #12825 second tranche: the remaining `cmd_status`
      orchestration moved into an OOP-first command-handler boundary in
      `commands_status.py` (`StatusCommandHandler` consuming a typed
      `StatusCommandRequest`, the cockpit projection behind `StatusCockpitMembershipPort`
      / `LiveStatusCockpitMembership`, a pure byte-preserving `render_status_report`,
      and a typed `StatusReport` result). `cmd_status` is now a thin adapter:
      resolve the session, drive the handler, print the report text, delegate the
      doctor tail. The `_status_repo_cockpit_membership` projection body and the
      `cmd_doctor` tail stay residual to #12638; the handler reaches the former via
      the live adapter (call-time `commands.*` routing, so the cockpit-membership
      integration tests are unchanged). `test_status_session_use_case` adds the
      fake-policy handler + pure-renderer specifications.
      Lowered 5984 -> 5931 for the #12830 tranche: the `_status_repo_cockpit_membership`
      projection body was removed from here, decomposed into the typed boundary in
      `commands_status.py` (the `CockpitMembershipIdentity` value object, the pure
      `match_cockpit_membership` domain policy, the `StatusCockpitMembershipReads`
      port + `LiveStatusCockpitMembershipReads` adapter, and the tolerant
      `CockpitMembershipProjection` use case). `LiveStatusCockpitMembership` keeps the
      same `StatusCockpitMembershipPort` shape and the live reads still route through
      the `commands.*` cockpit helpers at call time, so the cockpit-membership
      integration tests are unchanged. Only the `cmd_doctor` tail now stays residual
      to #12638. `test_status_session_use_case` adds fake-reads-port projection +
      pure match-policy specifications (one fewer `commands.*` monkeypatch dependency).
    owner_issue: "#12321"
    resolution_version: "v0.10.10 (Version #241)"
    expires: "2027-11-30"

  - path: src/mozyo_bridge/e_120_operations_cockpit/f_140_presentation_grouping_layout/domain/cockpit_layout.py
    lines: 2482
    reason: >-
      Cockpit layout geometry/projection domain; dense but cohesive. Candidate
      to split along geometry vs projection responsibilities. Path re-keyed from
      ``features/operations_cockpit/domain/cockpit_layout.py`` for the #12625
      Redmine-numbered layout move (old path kept as a sys.modules facade); the
      re-key lands in the same commit as the move per #12591 j#65488 atomicity.
      The module is byte-unchanged and the split itself is still owner #12321 scope.
      Raised 2344 -> 2438 for the #12658 project-scoped cockpit identity additive
      fields (`@mozyo_project_scope`/`@mozyo_project_path`/`@mozyo_project_label`
      option constants, the project fields on `CockpitWorkspace`/`CockpitPane` +
      their `as_dict` projections, and the project stamps in
      `pane_identity_commands`). Additive only — single-repo columns are
      byte-identical to pre-#12658.
      Raised 2438 -> 2456 for the #12658 j#66505 fix: a `launch_cwd` field on
      `CockpitWorkspace` + the `_launch_cwd` helper routes every pane-launch `-c`
      to the project workdir for a project-scoped column (repo_root identity
      unchanged), so a `--target-project` handoff to such a pane can pass.
      Raised 2456 -> 2482 for the #12658 j#66513 fix: a `@mozyo_repo_root` option
      constant + its stamp in `pane_identity_commands` so target discovery
      preserves the parent workspace identity for a project-scoped pane whose cwd
      is the project workdir.
    owner_issue: "#12321"
    resolution_version: "v0.10.10 (Version #241)"
    expires: "2027-11-30"

  - path: src/mozyo_bridge/application/doctor.py
    lines: 1762
    reason: >-
      Aggregated health/diagnostic collectors for `mozyo-bridge doctor`; grows
      with each new diagnostic section.
      Lowered 1852 -> 1831 for the #12833 doctor command body boundary tranche:
      the `run_doctor` body's two responsibilities — the section orchestration
      (external reads) and the authority-bearing health verdict (which becomes
      the doctor exit code) — moved behind the typed boundary in
      `doctor_health.py` (the `DoctorHealthVerdict` value object, the pure
      `evaluate_doctor_health` domain policy, the `DoctorSectionReads` port +
      `LiveDoctorSections` adapter, and the `RunDoctorUseCase`). `run_doctor`
      is now a thin handler composing the use case; `LiveDoctorSections` drives
      the section collectors through the `doctor` module at call time, so the
      existing `patch(...doctor.doctor_*_section)` integration tests are
      unchanged. `BAD_SECTION_STATUSES` is re-exported from `doctor_health`
      (`doctor_instruction` importer unchanged). `test_doctor_health` adds the
      fake-port use-case + pure verdict-policy specifications (the verdict
      authority no longer needs section-collector monkeypatching).
      Lowered 1831 -> 1797 for the #12835 doctor section collector boundary
      tranche: the `doctor_claude_launch_policy_section` collector's two
      responsibilities — the external read (`describe_launch_policy`) and the
      authority-bearing verdict (section status + next_action) — moved behind
      the typed boundary in `doctor_launch_policy.py` (the
      `LaunchPolicySectionVerdict` value object, the pure
      `evaluate_launch_policy_section` domain policy, the `LaunchPolicyReads`
      port + `LiveLaunchPolicyReads` adapter, and the
      `LaunchPolicySectionUseCase`). `doctor_claude_launch_policy_section` is
      now a thin handler composing the use case at call time, so the existing
      env-patching section tests are unchanged. `test_doctor_launch_policy`
      adds fake-port use-case + pure verdict-policy specifications (the verdict
      authority no longer needs `os.environ` patching). The remaining broad
      `doctor_*_section` collector bodies stay residual to #12638.
      Lowered 1797 -> 1795 for the #12836 doctor section collector boundary
      tranche: the `doctor_codex_skill_section` collector's three
      responsibilities — the external filesystem read (`codex_skill_home` /
      `_check_skill_dir`), the authority-bearing verdict (section status +
      next_action), and the legacy section dict assembly — moved behind the
      typed boundary in `doctor_codex_skill.py` (the `CodexSkillSectionVerdict`
      value object, the pure `evaluate_codex_skill_section` domain policy, the
      `CodexSkillReads` port + `LiveCodexSkillReads` adapter, and the
      `CodexSkillSectionUseCase`). `doctor_codex_skill_section` is now a thin
      handler composing the use case at call time; `LiveCodexSkillReads`
      resolves the `doctor` filesystem helpers and the shared
      `CODEX_SKILL_INSTALL_HINT` guidance (still imported by
      `doctor_instruction`) at call time, so the existing `CODEX_HOME`-seeding
      section tests are unchanged. `test_doctor_codex_skill` adds fake-port
      use-case + pure verdict-policy specifications (the verdict authority no
      longer needs temp-dir skill seeding or env patching). The remaining broad
      `doctor_*_section` collector bodies stay residual to #12638.
      Lowered 1795 -> 1762 for the #12843 doctor section collector boundary
      tranche: the `doctor_claude_skill_section` collector's three
      responsibilities — the external filesystem reads across three surfaces
      (`claude_skill_global_home` / `claude_skill_project_dir` /
      `claude_plugin_skill_root` / `_check_skill_dir` / `_check_plugin_install`),
      the authority-bearing verdict (section status + precedence warnings +
      next_action), and the legacy section dict assembly — moved behind the
      typed boundary in `doctor_claude_skill.py` (the
      `ClaudeSkillSectionVerdict` value object, the pure
      `evaluate_claude_skill_section` domain policy that owns the
      global/project/plugin precedence rules, the `ClaudeSkillReads` port +
      `LiveClaudeSkillReads` adapter, and the `ClaudeSkillSectionUseCase`).
      `doctor_claude_skill_section` is now a thin handler composing the use case
      at call time; `LiveClaudeSkillReads` resolves the `doctor` filesystem
      helpers and the shared `CLAUDE_GLOBAL_SKILL_INSTALL_HINT` guidance (still
      imported by `doctor_instruction`) at call time, so the existing
      `MOZYO_BRIDGE_CLAUDE_HOME` / `MOZYO_BRIDGE_CLAUDE_PROJECT_DIR`-seeding
      section tests are unchanged. `test_doctor_claude_skill` adds fake-port
      use-case + pure verdict-policy specifications (the verdict authority no
      longer needs temp-dir skill seeding or env patching). The remaining broad
      `doctor_*_section` collector bodies stay residual to #12638.
    owner_issue: "#12321"
    resolution_version: "v0.10.10 (Version #241)"
    expires: "2027-11-30"

  - path: src/mozyo_bridge/e_130_governance_distribution/f_160_release_version_governance/application/release.py
    lines: 1470
    reason: >-
      Release check/bump/publish/workflow helpers in one module. Splitting
      touches release gates, so it is sequenced carefully. Path re-keyed from
      ``application/release.py`` for the #12590/#12594 governance_distribution
      slug-layout move (old path kept as a sys.modules facade); re-keyed again
      to the Redmine-numbered Epic/Feature path for the #12622/#12626 layout
      correction (byte-identical move, line count unchanged), landing in the
      same commit as the move per the #12591 j#65488 / #12624 atomicity rule.
    owner_issue: "#12321"
    resolution_version: "v0.10.10 (Version #241)"
    expires: "2027-11-30"

  - path: src/mozyo_bridge/e_110_execution_platform/f_130_handoff_routing/domain/handoff.py
    lines: 1706
    reason: >-
      Handoff routing/kind/mode domain core. Cohesive but over threshold.
      Relocated move-only from `src/mozyo_bridge/domain/handoff.py` to the
      Redmine-numbered execution_platform layout by #12624 (US #12622); line
      count unchanged, legacy path preserved as a `sys.modules` facade. Self-
      module baseline re-point per #12623 conflict-point policy; foreign drift
      and final module_health integration remain #12630's.
      Baseline raised 1084 -> 1151 for the #12388 role-profile fields on the
      delivery outcome / notification body / delivery record (the resolved-text
      rendering stays out of the auto-persist body).
      Raised 1151 -> 1262 for the #12580/#12581 queue-enter Enter-only retry
      config boundary: the `QUEUE_ENTER_RETRY_*` constants, the
      `QueueEnterRetryPolicy` / `resolve_queue_enter_retry_policy` resolution
      seam (ready for future config-file driving), the durable-record-safe
      `QueueEnterRetryOutcome`, and the `- Retry:` line in
      `build_delivery_record`.
      Raised 1262 -> 1449 for the #12597 standard_target_admission config
      boundary: the `STANDARD_TARGET_ADMISSION_*` constants, the
      `StandardTargetAdmissionPolicy` / `resolve_standard_target_admission_policy`
      seam, the pure `StandardTargetAdmission` /
      `evaluate_standard_target_admission` minimal-admission evaluator, the
      durable-record-safe `TargetActivationOutcome`, and the `- Activation:` line
      in `build_delivery_record`.
      Raised 1449 -> 1499 for the #12706 ticketless transition role/action
      boundary wiring: the `transition_role` field on `DeliveryOutcome` /
      `make_outcome`, the single-line pointer clause in `build_notification_body`,
      and the `_transition_role_lines` renderer + `- Transition role:` block in
      `build_delivery_record`. The boundary values + resolver live in the separate
      `domain/transition_role.py` (kept out of this module), so only the
      outcome/record plumbing lands here.
      Raised 1499 -> 1561 for the #12700 workflow-contract reference wiring: the
      `workflow_contract` field on `DeliveryOutcome` / `make_outcome`, the
      single-line pointer clause in `build_notification_body`, and the
      `_workflow_contract_lines` renderer + `- Workflow contracts:` block in
      `build_delivery_record`. The bundle values + resolver live in the separate
      `domain/workflow_contract.py` (kept out of this module), so only the
      outcome/record plumbing lands here.
      Raised 1561 -> 1680 for the #12703 ticketless no-anchor callback rail: the
      `SOURCE_TICKETLESS` token + the `TicketlessAnchor` value object (so the
      standard marker/body/outcome rail works without a fabricated ticket anchor),
      the `ticketless_callback` field on `DeliveryOutcome` / `make_outcome`, the
      ticketless lead-sentence + pointer clause in `build_notification_body`, and
      the `_ticketless_callback_lines` renderer + `- Ticketless callback:` block in
      `build_delivery_record`. The callback values + worker-dispatch refusal live
      in the separate `domain/ticketless_callback.py` (kept out of this module), so
      only the anchor/outcome/record plumbing lands here.
      Raised 1680 -> 1691 for the #12705 LLM-facing q-enter / submit-complete
      additive `submit_lines` kwarg on `build_delivery_record` (the front door's
      pre-rendered composer-residue + delivery-id block; the `DeliveryOutcome` wire
      is unchanged). The residue classification + delivery-id derivation live in the
      separate `domain/q_enter.py` (kept out of this module).
      Raised 1691 -> 1809 for the #12740 forward ticketless no-anchor consultation
      rail: the `TicketlessConsultationAnchor` value object (so the standard
      marker/body/outcome rail works for the FORWARD leg without a fabricated ticket
      anchor), the `ticketless_consultation` field on `DeliveryOutcome` /
      `make_outcome`, the consultation/callback lead-sentence discrimination +
      pointer clause in `build_notification_body`, the forward-vs-callback
      discrimination in `_anchor_pointer_or_dash`, and the
      `_ticketless_consultation_lines` renderer + `- Ticketless consultation:` block
      in `build_delivery_record`. The forward consultation values + worker-dispatch
      anchor invariant live in the separate `domain/ticketless_consultation.py`
      (kept out of this module), so only the anchor/outcome/record plumbing lands
      here.
      Raised 1809 -> 1939 for the #12748 forward ticketless parent -> child
      work-intake rail: the `TicketlessWorkIntakeAnchor` value object (so the
      standard marker/body/outcome rail works for the parent -> child leg without a
      fabricated ticket anchor), the `ticketless_work_intake` field on
      `DeliveryOutcome` / `make_outcome`, the work-intake noun branch + pointer
      clause in `build_notification_body`, the work-intake discrimination in
      `_anchor_pointer_or_dash`, and the `_ticketless_work_intake_lines` renderer +
      `- Ticketless work-intake:` block in `build_delivery_record`. The work-intake
      values + invariants (worker dispatch stays anchor-gated, parent must not answer
      domain, child owns the anchor decision) live in the separate
      `domain/ticketless_work_intake.py` (kept out of this module), so only the
      anchor/outcome/record plumbing lands here.
      Lowered 1939 -> 1706 by #12750: the three ticketless no-anchor anchor models
      (`SOURCE_TICKETLESS` + `TicketlessAnchor` / `TicketlessConsultationAnchor` /
      `TicketlessWorkIntakeAnchor`) moved to the new `domain/ticketless_anchors.py`,
      and the ticketless record rendering (`_ticketless_{callback,consultation,
      work_intake}_lines` + the `_anchor_pointer_or_dash` ticketless branch) moved to
      the new `domain/ticketless_record_rendering.py`. `handoff.py` imports and
      re-exports the anchor symbols (identity preserved for `commands.py` / the
      ticketless tests) and delegates the rendering, so the Redmine-anchored vs
      ticketless-no-anchor boundary is now physically explicit and this module no
      longer grows per ticketless field.
    owner_issue: "#12321"
    resolution_version: "v0.10.10 (Version #241)"
    expires: "2027-11-30"

  - path: src/mozyo_bridge/scaffold/rules.py
    lines: 1081
    reason: >-
      Scaffold preset/rule installation logic. Just over threshold. Grew in
      #12363 for the opt-in sublane-flow profile (category registration, the
      router-variant template selector, and the sublane_flow_enabled toggle).
    owner_issue: "#12321"
    resolution_version: "v0.10.10 (Version #241)"
    expires: "2027-11-30"
