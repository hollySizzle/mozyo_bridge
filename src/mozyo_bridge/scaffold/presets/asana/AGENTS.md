# AGENTS

This project uses mozyo-bridge scaffold rules.

## Session Bootstrap

1. Confirm the current working directory is this project or a child of it.
2. Read the central mozyo-bridge preset rules:
   - `${rule_path}`
3. Read project-local context files listed below, if they exist.
4. Confirm the active Asana task before doing non-trivial work.

## Preset

- Ticket system: `${preset}`
- Preset version: `${preset_version}`
- Generated by: `mozyo-bridge ${mozyo_bridge_version}`

## Project-Local Context

- `README.md`
- `vibes/docs/specs/project-map.md`
- `vibes/docs/rules/agent-workflow.md`

## Guardrails

- Do not pretend to have read `${rule_path}` if it is unavailable.
- Asana task state and task comments are durable work state. Pane messages and chat messages are notifications only.
- Do not record credentials, tokens, or personal data in repository files or task comments.
