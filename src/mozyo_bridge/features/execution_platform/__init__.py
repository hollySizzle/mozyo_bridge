"""``execution_platform`` epic-slug package (Redmine Epic #12501, order 110).

Holds Feature-level sub-packages for the execution-platform bounded context as the
US #12570 source-layout pilot resolves the ``execution_platform`` big-box into
import-safe Feature slugs. Epic/Feature → slug mapping (with Redmine numeric
ordering metadata) is the ``vibes/docs/specs/bounded-context-map.md`` authority.
"""
