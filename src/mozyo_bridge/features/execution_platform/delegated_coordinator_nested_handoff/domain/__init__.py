"""``domain`` layer leaf of the ``delegated_coordinator_nested_handoff`` feature-slug package.

R1 layer-leaf shape (Redmine #12590/#12591, #12591 j#65435): real modules whose
legacy import path was ``mozyo_bridge.domain.<module>`` live here; the legacy path
stays available as a ``sys.modules`` facade (#12592 execution_platform move).
"""
