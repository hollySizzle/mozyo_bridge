"""``f_130_module_health`` application layer (Redmine Feature #12532, Epic #12505).

Application / CLI composition for module-level governance: the ``mozyo-bridge health``
argument parser (``cli_module_health``) and its command handlers
(``commands_module_health``). Relocated from
``features/quality_architecture/application`` by #12628 (Redmine-numbered layout
correction); the legacy ``mozyo_bridge.application.<module>`` import paths stay live
as ``sys.modules`` facades.
"""
