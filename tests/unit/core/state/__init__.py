"""unit — ``mozyo_bridge.core.state`` (shared-kernel state stores) tests.

Sibling marker to ``tests/unit/core/__init__.py``; see that module for why both were needed and
what the restoration measured. Discovery needs a package at every level, so the parent marker alone
does not bring these 7 files (116 tests) into ``discover -s tests``.
"""
